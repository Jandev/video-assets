# .NET
bin/
obj/
*.user
*.suo
*.nuget.props
*.nuget.targets
project.lock.json
project.fragment.lock.json
artifacts/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.uv/
uv.lock.bak
*.egg
.tox/
.coverage
htmlcov/

# Node
node_modules/
npm-debug.log*
yarn-error.log*

# Downloaded tool binaries — fetched by a demo's scripts, never source
.sbom-tool-dl/

# Aspire / container tooling
.aspire/
*.aspire-manifest.json

# Bicep / ARM build output
*.json.tmp
*.compiled.json

# Logs, temp and test artefacts generated while verifying demos
*.log
*.tmp
TestResults/
tmp/

# Editors and OS cruft
.vscode/
.idea/
.DS_Store
Thumbs.db

# Local secrets - never commit these
.env
.env.local
*.local.bicepparam
azure.local.json
# Generated by the demo deploy scripts: endpoints, written next to the app
appsettings.local.json
appsettings.*.local.json
*.pfx
*.pem
*.key

# Deploy outputs written by each demo's scripts/deploy.* - endpoints rather than
# secrets, but machine-specific and regenerated on every deploy.
.azure-outputs.env
.azure-outputs.ps1

# scripts/verify.sh scratch directory
.verify-work/
